# ARCHIVO .HTACCESS PARA MIVENTECH
# Configuraciones del servidor Apache para SEO, seguridad y rendimiento

# ===== PERMITIR ACCESO A ROBOTS.TXT Y SITEMAP.XML =====
# Estos archivos son esenciales para Google y motores de búsqueda
<FilesMatch "^(robots\.txt|sitemap\.xml|manifest\.json)$">
    Order Allow,Deny
    Allow from all
    # Headers específicos para sitemap
    <IfModule mod_headers.c>
        Header set Content-Type "application/xml; charset=utf-8"
    </IfModule>
</FilesMatch>

# Permitir acceso a archivos de verificación de Google
<FilesMatch "^google[a-z0-9]+\.html$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# ===== PROTECCIÓN DE ARCHIVOS SENSIBLES =====
# Bloquear acceso a archivos que contienen información sensible
<FilesMatch "\.(json|log|bak|inc|sh)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# ===== PERMITIR ARCHIVOS PÚBLICOS =====
# Archivos que los visitantes pueden descargar normalmente
<FilesMatch "\.(html|css|js|jpg|jpeg|png|gif|svg|ico|woff|woff2|ttf|webp|pdf)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# ===== PROTEGER DIRECTORIO DATA =====
# Bloquear acceso directo al directorio de datos
<Directory "data">
    Order Deny,Allow
    Deny from all
</Directory>

# ===== HABILITAR PHP =====
# Permitir ejecución de archivos PHP (para contact.php)
<FilesMatch "\.php$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# ===== CONFIGURACIÓN DE COMPRESIÓN GZIP =====
# Mejora la velocidad de carga del sitio web
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
</IfModule>

# ===== CONFIGURAR CACHÉ DEL NAVEGADOR =====
# Mejora la velocidad de carga en visitas repetidas
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Imágenes - cachear por 1 mes
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    
    # CSS y JavaScript - cachear por 1 semana
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
    
    # Fuentes - cachear por 3 meses
    ExpiresByType font/woff2 "access plus 3 months"
    ExpiresByType font/woff "access plus 3 months"
    ExpiresByType font/ttf "access plus 3 months"
    
    # HTML - cachear por 1 hora
    ExpiresByType text/html "access plus 1 hour"
</IfModule>

# ===== REDIRECCIONES IMPORTANTES =====
# Activar motor de reescritura de URLs
RewriteEngine On

# Redireccionar index.php a la raíz (si existiera)
RewriteCond %{THE_REQUEST} /index\.html[\s?] [NC]
RewriteRule ^index\.html$ / [R=301,L]

# ===== SEGURIDAD ADICIONAL =====
# Ocultar listado de directorios
Options -Indexes

# Ocultar información del servidor
ServerSignature Off

# Prevenir acceso directo al archivo .htaccess
<Files .htaccess>
    Order Allow,Deny
    Deny from all
</Files>

# ===== PÁGINAS DE ERROR PERSONALIZADAS =====
# Redirigir errores 404 a la página principal (para SPA)
ErrorDocument 404 /index.html
